---
description: Common practices, conventions, and context for the project
alwaysApply: true
---

# Common Practices and Conventions

## Project Context

源代码之外的项目相关文档统一使用 Markdown 格式或约定的其他格式。

项目上下文的描述通常应写在项目根目录的 [README.md](../../README.md) 中；在根目录或 `.cursor/rules/` 下也可放置项目专属规则。README 中建议包含如下内容：

- **项目概述**：项目用途、目标用户、核心价值、快速开始、构建与运行、关键链接
- **技术栈**：主要语言、框架、运行时、构建工具
- **架构设计**：关键决策、单体/微服务、模块划分、扩展点（如插件、中间件）、数据流
- **领域概念**：业务/领域术语及缩写，便于 AI 与新人理解
- **变更与任务**：CHANGELOG、TODO/ROADMAP 等

## Naming Conventions

### Files and Directories

文件和目录在命名习惯上，按语言与框架惯例选择，并保持统一：

- **模块/包**：命名习惯与架构一致，如 `module_name`、`ModuleName`, 不要使用短横线（-）作为连词符号。
- **源文件**：语言惯例（如 `.ts`/`.tsx`、`.py`、`.cpp`/`.hpp`、`.go`）
- **配置**：`.json`、`.yaml`、`.env`、`.ini`、`config/` 等，命名如 `config.*` 或按环境区分
- **文档**：`.md` 等，如 `README.md`、`CHANGELOG.md`、`docs/`

### Code Naming

常见约定（按项目/语言选定一种并统一）：

- **类型/类/接口**：PascalCase 或 UpperCamelCase
- **函数/方法**：camelCase 或 snake_case（与语言生态一致）
- **变量/参数**：camelCase 或 snake_case
- **成员变量**：可选后缀/前缀（如 `_private`、`m_`、尾随 `_`）以区分
- **常量**：UPPER_SNAKE_CASE 或与项目约定一致
- **命名空间/包/模块**：小写或 PascalCase，与生态一致
- **宏**（若使用）：UPPER_SNAKE_CASE

### 特殊模式

- **单例/全局入口**：统一命名（如 `getInstance()`、`instance`、`s_System`），在项目规则中明确
- **入口/ID 常量**：集中定义，命名表达含义（如 `MainEntryID`、`ROOT_MODULE`）

## Code Organization

### 关注点分离

- **公开 API vs 实现**：对外接口与内部实现分离
- **按层次/功能分目录**：如 `src/`、`include/`、`internal/`、`public/`、按功能子目录
- **接口与实现**：接口/抽象在一处，实现可分层或按模块

### 依赖与引用顺序（以 C/C++ 为例）

1. 预编译头（若有，且必须首位）
2. 标准库 / 语言运行时
3. 第三方库
4. 项目内其他模块
5. 当前模块或相对路径头文件

其他语言：遵循「标准库 → 第三方 → 项目内」的惯例，并保持 import/require 分组与排序一致。

### 头文件与依赖（若适用）

- 在头文件中用前向声明减少编译依赖
- 仅在需要完整类型时包含定义
- 头文件保护：`#pragma once` 或 include guard，项目内统一

## Documentation Standards

### 代码注释

- 公开 API：用块注释说明用途、参数、返回值、异常与约束
- 复杂逻辑：简短说明「为什么」而不只是「做什么」
- 行内注释：仅在不言自明处省略

### 文件头（可选模板）

```text
/**
 * @file FileName.ext
 * @brief 一句话说明文件职责
 * @details 可选：详细说明、依赖关系、使用场景
 */
```

## Version Control

### 工作流

- **分支策略**：按团队约定（如 main/develop、Git Flow、 trunk-based）
- **提交信息**：简洁、可检索，说明「做了什么/为什么」（可约定格式如 Conventional Commits）
- **忽略规则**：用 `.gitignore`（及 `.cursorignore` 若需）排除构建产物、本地配置、临时文件

### 纳入与排除

- 纳入：源码、可复现的配置模板、文档、脚本
- 排除：构建输出、依赖目录、本地密钥、IDE 本地配置（若团队不共享）

## Common Patterns

### 资源与生命周期

- 优先使用语言/框架提供的资源管理（智能指针、with/using、defer、context）
- 采用 RAII 或等价模式，确保申请与释放成对、顺序明确
- 避免在业务逻辑中散落 `new/delete` 或裸句柄

### 错误处理

- 约定返回值语义（如 bool、Result、Optional）或异常使用范围
- 在边界处校验输入、校验失败时尽早返回或抛出
- 记录足够上下文（日志级别、错误码、关键参数），便于排查

### 初始化与清理

- **初始化**：先“依赖方”,然后“本方”，显式顺序（配置 → 基础设施 → 业务）
- **清理**：与初始化顺序相反，在析构或统一 shutdown 中执行
- 避免在静态析构阶段依赖已销毁的全局对象

### 并发与共享状态

- 明确哪些状态是共享的、由谁写
- 使用合适的同步原语或不可变/线程安全结构
- 避免锁顺序不一致导致的死锁；高并发路径可考虑无锁或消息传递

## Anti-Patterns to Avoid

### 资源与内存

- ❌ 用裸指针表示所有权、手动成对 new/delete
- ❌ 未在错误路径或析构中释放资源
- ✅ 使用智能指针/RAII/语言惯用方式
- ✅ 在单测或 CI 中做泄漏检测

### 并发

- ❌ 无保护的共享可变状态
- ❌ 锁顺序不一致、忽略超时与中断
- ✅ 明确并发模型，使用标准同步手段或消息传递
- ✅ 对关键路径做并发测试与压力测试

### 性能与复杂度

- ❌ 无测量的「预感式」优化
- ❌ 不必要的拷贝、不恰当的数据结构
- ✅ 先测量再优化，保留基准与监控
- ✅ 使用移动语义、视图、惰性计算等语言/库特性

## Debugging Guidelines

### 日志

- 约定日志级别（Trace/Debug/Info/Warning/Error）及使用场景
- 日志包含请求/会话/模块等上下文，便于过滤与追踪
- 生产环境默认级别与采样策略在配置中明确

### 工具与环境

- 使用项目统一的 IDE/编辑器与调试器
- 保证符号与源码映射（如 PDB、source map、debug 构建）
- 需要时使用性能/内存分析器定位瓶颈与泄漏

### 常见问题（按项目补充）

- 依赖/头文件顺序、宏冲突、ABI/架构一致性
- 环境变量、工作目录、多环境配置差异
- 在项目规则或 README 中维护「常见问题与解决」

## Testing Guidelines

### 单元测试

- 对核心逻辑和公共 API 编写单测，尽量隔离外部依赖
- 覆盖正常路径与典型错误路径
- 测试保持快速、稳定、可重复，命名表达意图

### 集成与端到端

- 验证模块/服务间交互与关键流程
- 包含失败与恢复场景（超时、断网、错误响应）
- 使用测试环境与可重复数据，避免依赖生产

### 性能与回归

- 对关键路径做基准或性能测试
- 在 CI 中做回归对比（可选），并设定合理阈值

## Performance Considerations

### 内存

- 热点路径减少分配、复用缓冲与对象池
- 考虑数据布局与缓存友好性（若涉及高性能计算）

### CPU

- 避免不必要的拷贝与重复计算
- 选择合适的数据结构与算法复杂度
- 在测量基础上优化热点，保持可读性

### I/O 与网络

- 控制请求大小与数量，合理批处理与分页
- 使用高效序列化与压缩（若适用）
- 设置超时、限流与背压策略

## Security Considerations

### 输入与输出

- 校验与规范化所有外部输入（网络、文件、环境）
- 输出时避免注入（如转义、参数化、安全 API）
- 检查边界与长度，防止溢出与畸形数据

### 资源与权限

- 设置超时与资源上限，防止 DoS 与资源耗尽
- 最小权限原则，敏感操作需鉴权与审计
- 密钥与凭证不入库、不写死，使用环境或密钥管理

### 通信与存储

- 传输与存储敏感数据时使用 TLS/加密与安全协议
- 使用安全随机数、合规的哈希与算法
- 按需脱敏日志与错误信息

## Team Collaboration

### Code Review

- 关注正确性、可读性、测试与文档
- 检查是否遵循项目约定与常见陷阱
- 反馈具体、可操作，区分必须修改与建议

### 沟通与一致性

- 重要设计决策记录在文档或 ADR 中
- 代码与文档同步更新
- 提交信息、分支与发布流程与团队约定一致

### 一致性

- 命名、目录结构、错误处理方式在项目内统一
- 新代码遵循既有模式，必要时先更新约定再推广

## Development Environment

### IDE / 编辑器

- 使用团队约定的 IDE 或编辑器及推荐插件
- 配置统一的格式化、Lint 与缩进（如 EditorConfig）
- 确保能一键构建、运行与调试

### 构建与 CI

- 构建命令与依赖安装方式写在 README 或脚本中
- CI 执行 lint、测试与（可选）构建，失败即阻断
- 构建环境可复现（版本固定、容器或文档化）

### 常用工具

- 版本控制、包管理、测试与格式化工具在文档中列出
- 新成员可据此快速搭好环境

## Best Practices Summary

1. **可读优先**：代码被阅读的次数远多于编写，命名与结构要清晰
2. **遵循项目约定**：命名、目录、错误处理与团队规则一致
3. **文档化对外契约**：公共 API、配置项与部署方式要有说明
4. **可测与可测性**：核心逻辑可测，测试稳定且有意义
5. **先测再优化**：用数据指导优化，保留基准与监控
6. **显式错误处理**：边界与错误路径明确，便于运维与排查
7. **单一职责**：函数/模块职责清晰，便于复用与测试
8. **适度抽象**：在简洁与扩展性之间取得平衡，避免过度设计
9. **性能与可维护性平衡**：不为一味追求性能牺牲可读性与安全性
10. **持续改进**：通过 Review、重构与文档迭代提升代码与流程
